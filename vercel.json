{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "node scripts/prepare-prisma-for-postgres.mjs && prisma generate --schema=prisma/schema.postgresql.prisma && prisma db push --accept-data-loss --skip-generate --schema=prisma/schema.postgresql.prisma && next build",
  "installCommand": "npm ci --no-audit --no-fund",
  "regions": ["iad1"],
  "github": {
    "silent": true
  }
}
